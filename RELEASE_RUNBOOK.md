# app-tai 发布与回滚 Runbook

> 适用范围：`packages/app-tai`（Telegram Mini App 前端）及其依赖 API。
> 最后更新：2026-02-09

## 1. 角色与职责

| 角色 | 执行人 | 主要职责 | 备份 |
|---|---|---|---|
| 发布经理 | 当周值班 PM/Tech Lead | 全流程 Go/No-Go 决策、进度同步 | 项目经理 |
| 前端发布人 | 当周值班前端 | 构建、部署、前端回滚 | 前端负责人 |
| 后端值班人 | 当周值班后端 | API 可用性、后端回滚、限流与告警处理 | 后端负责人 |
| 合约值班人 | 当周值班合约 | 链上异常研判、合约交互降级决策 | 合约负责人 |
| QA 值班人 | 当周值班测试 | 关键路径回归、上线后 24h 巡检记录 | 测试负责人 |

## 2. 发布时间窗

- 常规发布时间：工作日 `02:00-04:00 UTC`（低峰窗口）。
- 冻结规则：发布前 30 分钟代码冻结，仅允许发布阻断级修复。
- 热修规则：非窗口发布必须由发布经理 + 后端值班人双确认。

## 3. 发布前检查（Preflight）

0. 主网安全门禁（强制）：
   - 未经业务负责人**明确授权**，禁止执行任何 Oracle 推送、价格写入、解锁相关操作。
   - 如需执行，必须先在发布单记录授权人、执行窗口、回滚方案。
1. 确认目标 commit 和变更说明，记录在发布单。
2. 校验环境变量：
   - `VITE_API_BASE` 指向生产 API。
   - 合约地址覆盖项与主网地址一致（如配置了 override）。
3. 在 `packages/app-tai` 执行：
   - `npm run release:check`
4. 检查 `public/tonconnect-manifest.json` 中 `url/iconUrl` 为生产域名。
5. 确认 Telegram Mini App 入口链接和 Bot 菜单已准备好。

## 4. 发布步骤（Frontend）

1. 标记发布版本（tag 或发布单编号），记录操作者和时间。
2. 构建产物：`npm run build`，确认 `dist/` 生成。
3. 按现网流程部署静态产物（CI 或手工上传 `dist/`）。
4. 部署后执行：
   - `npm run smoke`
5. 在 Telegram 内做最小回归：
   - 连接钱包
   - 购买档位（sale）
   - 发起存款（deposit）
   - 查看邀请数据
   - 查看奖励并发起领取
   - 查看排行榜

## 5. Go/No-Go 检查点

- `typecheck/build/smoke` 全部通过。
- 页面可打开且无白屏，关键路由可访问（`/home /sale /deposit /rewards`）。
- `/price` 或 `/api/price` 至少一个健康。
- Telegram 内关键链路 6 项通过，才允许 Go。

## 6. 回滚策略

### 6.1 前端回滚

1. 定位最近一个稳定版本（上一版发布记录）。
2. 重新部署上一版静态产物。
3. 5 分钟内验证 `/home` 可访问、钱包可连接、API 可读。
4. 在发布群公告“已回滚 + 原因 + 下一步”。

### 6.2 后端回滚

1. 由后端值班人回退到上一稳定 Worker/API 版本。
2. 优先恢复只读能力（价格、用户数据查询），再恢复交易写接口。
3. 记录回滚版本号与时间，追加到事故记录。

### 6.3 配置回滚

1. 恢复上一版环境变量快照（发布前必须备份）。
2. 重新构建并部署前端。
3. 验证合约地址、API Base、manifest URL 与预期一致。

### 6.4 合约交互降级

当链上或预言机异常时，按顺序降级：

1. 先关闭交易入口引流（Bot 菜单/入口文案提示维护中）。
2. 前端发布只读版本（隐藏或禁用购买、存款、领取按钮）。
3. 后端对签名/领取类接口返回维护态，保留查询接口可用。
4. 异常恢复后按“后端 -> 前端 -> 入口”顺序恢复。

## 7. 上线后 24h 巡检

- `T+15m`: 访问健康、错误日志、关键路由白屏检查。
- `T+1h`: API 成功率、前端 JS 错误率、核心转化漏斗。
- `T+4h`: Telegram 内完整链路回归 1 次。
- `T+12h`: 检查慢查询/超时峰值和异常告警闭环。
- `T+24h`: 输出巡检结论（通过/待修复）并归档。

## 8. 告警阈值（建议）

| 维度 | 阈值 | 持续时间 | 优先级 |
|---|---|---|---|
| 前端 JS 错误率 | > 2% | 5 分钟 | P1 |
| API 可用性 | < 99% | 5 分钟 | P1 |
| API p95 延迟 | > 2000ms | 10 分钟 | P2 |
| 购买成功率 | 较近 7 日均值下降 > 20% | 15 分钟 | P1 |
| 领取成功率 | 较近 7 日均值下降 > 20% | 15 分钟 | P1 |
| Telegram 打开失败率 | > 5% | 10 分钟 | P2 |

## 9. 发布记录模板

每次发布至少记录以下字段：

- 发布版本（commit/tag）
- 发布时间（UTC）
- 发布经理 / 操作者
- Go/No-Go 结论
- 回归结果（6 项关键链路）
- 是否回滚（原因、时间、处理人）
